spring:
  redis:
    host: jktu.cc
    port: 6379
  sql:
    init:
      schema-locations: classpath:/blog/schema.sql
      mode: always
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

mybatis-plus:
  global-config:
    banner: false